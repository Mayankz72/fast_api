# Build Log — FastAPI Docs RAG + Eval Harness

Running log of decisions, steps, and issues for this project. Updated as we go.
See `RESOURCES.md` for the research papers and engineering write-ups backing these decisions.

## Goal

A resume-worthy RAG project that stands out by including a real evaluation
harness (metrics, golden dataset, tracing) instead of just a "chatbot over docs"
demo. See `README.md` for the pitch and setup instructions.

## Decisions

| Decision | Choice | Why |
|---|---|---|
| Domain/corpus | FastAPI docs + source (`tiangolo/fastapi`) | Technically differentiated (code-aware chunking, multi-file context); meta angle of a FastAPI app about FastAPI |
| Golden dataset source | GitHub Discussions (Q&A category, answered only) | Real (question, ground-truth-answer) pairs for free, instead of hand-written/synthetic Q&A |
| LLM stack | ~~OpenAI `gpt-4o-mini` + `text-embedding-3-small`~~ → Gemini `gemini-3.5-flash-lite` (generation) + `gemini-embedding-001` (embeddings), judged by `gemini-3.1-flash-lite` in DeepEval | User has no budget for OpenAI billing; Google AI Studio's free tier covers this at $0. See Issues below for the free tier's real throughput constraints. |
| API keys | Two separate Google accounts' keys: `GEMINI_API_KEY` (generation + eval judge) and `GEMINI_API_KEY_EMBED` (embeddings only) | Each free-tier account/project gets its own independent quota buckets. Splitting embedding and generation traffic across two accounts means the (very tight) embedding throughput doesn't compete with the (separately tight) generation daily cap - see Issues. User's own accounts, disposed of after the project. |
| Embedding backend (dev) | Local `BAAI/bge-small-en-v1.5` via `sentence-transformers`, separate Qdrant collection `fastapi_corpus_local` | Gemini's free embedding tier is too slow to iterate against (see Issues) — local embeddings give an immediately-usable pipeline for development/demo, while the Gemini-embedded collection remains the "official" $0-API-stack version once it finishes indexing. Switch via `EMBED_BACKEND=local\|gemini` in `.env`. |
| Vector DB | Qdrant, via Docker | Production-grade over an in-memory store; one step from being deployable |
| Eval framework | DeepEval | pytest/CI-friendly — "tested like software," not a one-off notebook |
| Tracing | Arize Phoenix, run via Docker (not pip) | See Issues below — the pip package conflicts with Python 3.11 |

## Steps completed

1. Scaffolded project structure (`src/ingest`, `src/index`, `src/rag`, `src/eval`, `src/api`)
2. `src/ingest/fetch_docs.py` — shallow-clones the FastAPI repo, stages `docs/en/docs/*.md` and `fastapi/*.py`
   - Ran successfully: 155 markdown docs, 48 python source files staged
3. `src/ingest/chunk.py` — markdown-aware (header-path-preserving) chunking for docs, `ast`-based function/class chunking for source
   - Ran successfully: 2,280 doc chunks + 552 code chunks = 2,832 total
4. `src/ingest/fetch_discussions.py` — GraphQL pull of answered GitHub Discussions as golden Q&A pairs (needs `GITHUB_TOKEN`) — **not yet run** (GitHub PATs are free — create one at github.com/settings/tokens with `public_repo`/read scope, no billing involved)
5. `src/index/embed_and_index.py` — embeds all chunks via Gemini (`gemini-embedding-001`, 3072-dim) and upserts into Qdrant collection `fastapi_corpus` — **running in the background, will take hours** (see Issues)
   - `src/index/embed_and_index_local.py` — same corpus, embedded locally via `BAAI/bge-small-en-v1.5` (384-dim) into collection `fastapi_corpus_local` — **ran successfully, all 2,832 chunks indexed in ~6 min**
6. `src/eval/build_golden_dataset.py` — filters/formats fetched discussions into `data/processed/golden_dataset.json` — **not yet run** (blocked on step 4)
7. `src/rag/{retriever,generator,pipeline}.py` — retrieve-then-generate pipeline, `EMBED_BACKEND` env var picks local vs. Gemini retrieval — **smoke-tested successfully end-to-end against the local backend** (correct, grounded, sourced answer for "How do I define a path parameter with a type?")
8. `src/eval/run_deepeval.py` — scores the pipeline against the golden set on Faithfulness, Answer Relevancy, Contextual Precision, Contextual Recall, judged by `gemini-3.1-flash-lite` via DeepEval's native `GeminiModel` — **ran successfully, first real scores in.** Runs in small resumable batches (default 2/call) that append to `data/processed/eval_report.json` rather than overwrite, skipping already-scored questions — see Issues below for why.
   - **First 2/437 results:** Faithfulness and Answer Relevancy both 1.00 on both examples (generation stays grounded, no hallucination beyond retrieved context). Contextual Precision/Recall lower (0.375-1.00) — retrieval ranking/coverage is the current weak point, not generation. This is exactly the gap the planned Contextual Retrieval ablation (step 6 below) targets.
9. `src/eval/phoenix_tracing.py` — OTel + OpenInference instrumentation (`openinference-instrumentation-google-genai`) exporting traces to Phoenix — **not yet run**
10. `src/api/main.py` — FastAPI `/query` endpoint wrapping the pipeline
11. Docker Compose brings up Qdrant (`:6333`) and Phoenix (`:6006`) — both verified healthy
12. Git repo initialized, first commit made with the full scaffold

## Issues hit + resolutions

- **`arize-phoenix` pip package fails to import on Python 3.11.** `phoenix/__init__.py` (v20.8.0) pulls in code with a `dataclass` mutable-default pattern that only Python 3.13 allows; older versions (<8) require compiling `sqlean-py` from source (needs MSVC Build Tools, not installed); v12.35.0 installs but has a version-skew bug between core `arize-phoenix` and the separately-versioned `arize-phoenix-evals` package (`phoenix.evals.models` module missing).
  - **Fix:** dropped the `arize-phoenix` pip dependency entirely. Phoenix now runs as its own Docker container (`arizephoenix/phoenix:latest`, confirmed running Python 3.13 internally — validates the diagnosis). The app only needs the lightweight `opentelemetry-sdk` + `openinference-instrumentation-google-genai` packages to export traces to it over OTLP — no heavy/fragile phoenix package in the app's own environment.

- **Gemini AI Studio free tier for `gemini-embedding-001` throttles at ~1000 *tokens*/minute, not a clean requests-per-minute count.** Discovered empirically: isolated 1-5-chunk batches (a few hundred tokens) succeed reliably; 20-50-chunk batches (2,000+ tokens) 429 almost every time regardless of pacing, and the SDK's own hidden internal retry-on-429 was silently multiplying our real request count, compounding the problem. At this real throughput, embedding the full 2,832-chunk corpus takes multiple hours.
  - **Fix:** `embed_and_index.py` now (a) disables the SDK's internal retry (`HttpRetryOptions(attempts=1)`) so every HTTP call is visible and accounted for, (b) batches by a character budget (~2,400 chars/batch, capped for margin) instead of a fixed chunk count, since chunk length varies wildly (20–2,000+ chars) and a fixed count can randomly land on several long chunks, (c) paces requests ~75s apart, and (d) checkpoints progress to `data/processed/embed_checkpoint.json` after every successful batch so a rerun after hitting quota resumes instead of re-embedding (and re-spending quota on) already-indexed chunks.
  - **Workaround for iteration speed:** added `src/index/embed_and_index_local.py` using `BAAI/bge-small-en-v1.5` (free, offline, no rate limits) into a separate Qdrant collection (`fastapi_corpus_local`). `retriever.py` picks the backend via `EMBED_BACKEND=local|gemini`. Use `local` for day-to-day dev/demo; the Gemini-embedded collection is the "everything free, all-Gemini" version for the record, and finishes indexing unattended in the background.
  - Also hit: `gemini-2.5-flash-lite` and `gemini-2.5-flash` both returned 404 ("no longer available to new users") despite appearing in `models.list()` - generation now uses `gemini-3.5-flash-lite`.

- **Every free-tier Gemini *chat* model on this account caps at ~20 `generate_content` calls/day** (`GenerateRequestsPerDayPerProjectPerModel-FreeTier`), confirmed identically on both `gemini-3.5-flash` and `gemini-3.6-flash` - looks like a blanket account-level restriction, not something specific to one model, since a brand-new model showed the exact same "20" limit on its first use. There's also a separate, tighter per-minute cap (`gemini-3.5-flash`: 5/min) that DeepEval's default concurrent metric execution (`asyncio.gather` over all 4 metrics per test case, immune to the `async_config` throttle which only paces *between* test cases) blows through instantly regardless of the daily cap.
  - **Fix:** `run_deepeval.py` no longer calls DeepEval's `evaluate()`. It calls `metric.measure(test_case)` one metric at a time in a plain loop with explicit pacing (20s) and its own retry/backoff on both 429 (quota) and 503 (transient model overload - also observed once). Given the ~20/day ceiling and 4 metrics x 1-2 calls/metric, only ~2 golden examples fit per model per day - so the harness now processes a small batch (default 2) per invocation and **appends** to `eval_report.json`, skipping already-scored questions, so the eval set builds up over several days instead of failing to finish a big run in one. Settled on `gemini-3.1-flash-lite` as the long-term judge model (for consistent scoring across days) after `gemini-3.5-flash` and `gemini-3.6-flash` were both already spent on same-day testing.
  - Ran a second free Google account's key through the same accounting: also capped at 20/day on first use, confirming the account-wide (not per-model) theory. Now split across two accounts by *role* rather than trying to round-robin models: `GEMINI_API_KEY_EMBED` (fresh account) handles all embedding traffic, `GEMINI_API_KEY` (original account) handles all generation/judge traffic - see the API keys decision row above.
  - **Further hardening after running the eval loop unattended ("run until quota expires"):** hit two more failure modes that aren't quota-related and shouldn't kill the whole run: (a) DeepEval's per-attempt timeout (~88s) was too short for some larger-context judge calls - raised to 180s via `DEEPEVAL_PER_ATTEMPT_TIMEOUT_SECONDS_OVERRIDE`; (b) the judge model occasionally returns malformed JSON for its structured verdict output (`gemini-3.1-flash-lite` is a small/cheap model, this happens more than it would with a bigger judge) - DeepEval raises a plain `ValueError` for this, uncaught by the 429/503/timeout-specific handling. Now the per-test-case loop catches *any* exception other than a 429/503 quota error: quota errors stop the whole run (that example isn't marked scored, so it's retried fresh next time), anything else gets recorded as an `"error"` entry for that question (so it isn't retried forever) and the loop moves on to the next example. This is what makes an unattended "run until quota's actually gone" loop viable.

## Current state (as of 2026-09-07)

- Docker Desktop was not running after the restart (containers had exited, not removed); started Docker Desktop manually and `docker compose up -d` brought `project-qdrant-1` and `project-phoenix-1` back up healthy.
- All Python dependencies installed in `.venv` (incl. `google-genai`, `sentence-transformers`)
- Four Google accounts now split by role in `.env`: `GEMINI_API_KEY` + **new today** `GEMINI_API_KEY_2` (generation/judge), `GEMINI_API_KEY_EMBED` + `GEMINI_API_KEY_EMBED_2` (embedding workers). `run_deepeval.py` now takes `--api-key-env` (mirrors the flag `embed_and_index.py` already had) so either generation/judge account can be selected; it threads through `RAGPipeline(generator_api_key_env=...)` -> `Generator(api_key_env=...)`.
- Corpus cloned and chunked (2,832 chunks)
- `fastapi_corpus_local` (Qdrant, local `bge-small` embeddings): **fully indexed, ready to use** — this is the backend the RAG pipeline is smoke-tested against end-to-end
- **Root-cause correction on the 2026-09-06 "OS killed both processes for low memory" entry below:** the embed workers themselves are not inherently heavy (Gemini API calls only). The spike that day came from starting `run_deepeval.py` (loads `sentence-transformers`/torch) while free memory was already critically low (~1.5GB) - watched it happen live today: 1.5GB -> 850MB free in ~40s right after launching the eval loop next to a worker, stopped it before it crashed anything. **Revised takeaway (also confirmed today): it's about available headroom, not a strict "never run together" rule** - later the same day, worker B (embedding) and the eval loop (on the new `GEMINI_API_KEY_2` account) ran concurrently without issue once free memory was ~5GB at launch. Rule of thumb: check free memory before starting `run_deepeval.py`; don't launch it with less than ~2-3GB free.
- **Bug fixed today:** both `embed_and_index.py` and `run_deepeval.py` only caught `httpx.ConnectError` for transient-network retry, but `httpx.ConnectTimeout` is a sibling exception (not a subclass) and slipped through uncaught - crashed worker B mid-run after a real network blip, even though it had already made progress (8/176 batches) and was otherwise healthy. Fixed by catching `httpx.TransportError` (the common base class covering ConnectError, ConnectTimeout, ReadTimeout, etc.) in both scripts instead.
- `fastapi_corpus` (Qdrant, Gemini embeddings): **fully indexed - 2832/2832 points, done.** Split across two workers by chunk range (per commit `a14b715`): worker B (`GEMINI_API_KEY_EMBED_2`) finished its own [2086, 2832) range first (168 batches, ~3h36m, one more transient network blip near the end caught by the ConnectTimeout fix below). `GEMINI_API_KEY_EMBED` (worker A's own account) never reset that day despite being checked repeatedly, so once worker B's key freed up, worker A's remaining [1341, 2086) range (~38 chunks) was simply re-run on `GEMINI_API_KEY_EMBED_2` instead - checkpoints are key-agnostic (just track position) and point IDs are absolute-chunk-index-based, so reusing a different account for someone else's range is always safe, no collision risk.
  - `embed_checkpoint.json`/`_a.json`/`_b.json` are now all cleared (each script deletes its checkpoint on successful completion of its range) - safe to ignore/delete if still present.
- `EMBED_BACKEND=local` currently set in `.env` - now that `fastapi_corpus` (Gemini embeddings) is fully indexed too, worth re-running the eval with `EMBED_BACKEND=gemini` to compare against the local-embedding baseline (see next steps).
- Golden dataset built: 437 examples in `data/processed/golden_dataset.json`
- DeepEval harness (2026-09-07 -> 2026-09-08 overnight): started at 118 scored, ended at **369/437 scored (84.4%)**. Quota exhausted and recovered multiple times through the day (don't assume daily quotas take a full day to reset - sometimes reset within hours, sometimes a new calendar day doesn't immediately free a specific account either). Tried 2-way parallel sharding (see below) which didn't pan out; reverted to single-instance sequential account-chaining (`GEMINI_API_KEY_2` -> `GEMINI_API_KEY`), which is what actually carried it from 195 to 369.
- **Eval loop killed by the harness for low memory multiple times** during this stretch, down to as low as ~0.86GB free at one point - general system-wide memory pressure (many editor/browser windows open), not caused by these jobs. No data lost any time (checkpointed after every test case) - just kept relaunching the same chained command each time and it resumed from the checkpoint. Windows' own "Memory Compression" process running at ~1GB is a good signal the system is under real memory pressure, independent of anything this project is doing.
- **Tried 2-way parallel eval sharding** (added `--report-path`/`--start`/`--end` to `run_deepeval.py`, wrote `merge_shard_reports.py`) to use all 4 Google accounts (2 embed accounts freed up once indexing finished) instead of just the 2 generation/judge accounts. Looked stable for the first ~3 minutes of monitoring but the harness killed both instances a few minutes later for low memory - **not sustainable on this machine, reverted to single-instance operation.** The sharding code itself works and is still in the repo if a beefier machine wants to use it later; just don't run 2+ `run_deepeval.py` instances concurrently here.
- **Quota reset timing confirmed unreliable across accounts even across a calendar-day boundary:** `GEMINI_API_KEY_2` was still exhausted right after the date rolled over to 2026-09-08 (see below), while `GEMINI_API_KEY` worked. Don't assume a day-boundary reset applies to every account uniformly - probe each one.
- **Do not run two `run_deepeval.py` instances concurrently even on different accounts** - both read the full `eval_report.json`, append their batch, and overwrite the whole file, so a concurrent second writer can clobber the first's freshly-scored entries. Chain them sequentially instead (`... --api-key-env A || break` then repeat with `--api-key-env B`), which is what worked today.
- **Bug fixed today (2nd one):** `Generator._pick_model()` raised a bare unhandled `RuntimeError` when every candidate *generation* model was exhausted, producing an ugly traceback instead of the clean "exhausted for today" exit that the judge-model path already had (no data loss - it happens before any scoring - just a rough edge). `run_deepeval.py`'s `main()` now catches this around the generation step and exits cleanly with a matching message.
- Nothing blocked - everything running on $0 API cost, just rate-limited by free-tier quotas split across four Google accounts (2 embed + 2 generation/judge, though the embed ones are idle now that indexing is done). Both generation/judge accounts (`GEMINI_API_KEY`, `GEMINI_API_KEY_2`) are fully exhausted as of this writing; retry once quota resets.

## Current state (as of 2026-09-08, later same day)

- **DeepEval harness finished: 437/437 golden examples scored (100%).** Chained `GEMINI_API_KEY` -> `GEMINI_API_KEY_2` sequentially, batch sizes 5-10, memory holding steady around ~2.8GB free throughout - no crashes from that this time.
  - **Final v1 baseline (425/437 with metrics, 12 recorded as `"error"` entries - mostly judge-model malformed-JSON/timeout flakiness, not pipeline failures):** Faithfulness avg **0.949** (92.9% pass @0.7 threshold), Answer Relevancy avg **0.771** (68.7% pass), Contextual Precision avg **0.231** (17.6% pass), Contextual Recall avg **0.128** (6.4% pass). All-4-metrics-pass rate: **2.4%** (10/425). Consistent with the 266/437 partial read from earlier today - generation is solid, retrieval is the clear, confirmed bottleneck.
- **Bug fixed today (3rd one):** `run_deepeval.py` crashed with `UnicodeEncodeError` printing a golden question containing a full-width `？` - Windows' console defaults `sys.stdout` to cp1252, which can't encode it. No data lost (report is saved after each completed test case), but it killed the batch outright. Fixed by reconfiguring `sys.stdout` to UTF-8 with `errors="replace"` at import time.
- `GEMINI_API_KEY`'s judge-model quota (`gemini-3.1-flash-lite`) ran out partway through today's push (429 after ~9 batches); switched to `GEMINI_API_KEY_2` to finish the remaining ~23 examples rather than waiting on reset.
- **Fixed context ordering in `generator.py` per the Lost-in-the-Middle finding.** Added `reorder_lost_in_middle()`: interleaves the retrieved chunks from both ends inward (rank 0 first, rank 1 last, rank 2 second, ...) before building the prompt, so the most relevant chunks sit at the start/end of the context instead of getting buried in the middle. Only the prompt-facing copy is reordered - `RAGResult.contexts` (citations, and DeepEval's contextual-precision/recall metrics which specifically judge retrieval ranking) keeps the true relevance order. Smoke-tested against `EMBED_BACKEND=local` - still produces a correct, grounded, sourced answer. **Not yet re-scored against the full golden set** (generation-only change, didn't want to spend more judge quota re-running all 437 just for this before the bigger Contextual Retrieval change lands - the next full eval run will show its combined effect).

## Current state (as of 2026-09-09, overnight into 09-09)

- **Implemented Anthropic's Contextual Retrieval end to end.** New `src/index/contextualize_chunks.py` generates a short LLM-written context blurb per chunk (see RESOURCES.md). Anthropic's reference design is one LLM call per chunk (cheap there via prompt caching); our free tier is capped by call *count*, so this batches every chunk from the same source document into one call instead (sub-batched at 25 chunks/call for oversized files - `release-notes.md` alone is 37% of the corpus). Brought the whole corpus down to **244 calls instead of 2,832**.
  - **Contexts fully generated: 2,832/2,832 chunks, 244/244 calls - finished in a single run on `GEMINI_API_KEY_EMBED` alone**, no quota exhaustion at all. Output: `data/processed/chunk_contexts.json` (gitignored, chunk index -> context string).
  - Hardened along the way: judge occasionally returns the wrong array length (19 or 24 instead of 25) - now retried, and if a batch persists in failing it's skipped (not marked done) rather than crashing the whole run, matching the graceful-degradation pattern already used in `run_deepeval.py`.
  - Extended `embed_and_index.py` with `--context-file`: prepends each chunk's context before embedding (payload still stores the original uncontextualized `text` for citations/eval, plus the `context` string itself). Char-budget batching now sizes batches off the (longer) contextualized text. Fully backward compatible - omitting the flag reproduces exact v1 behavior.
- **Building the v2 index now: collection `fastapi_corpus_contextual`, split across two workers same as v1** (`GEMINI_API_KEY_EMBED` on chunks [0, 1416), `GEMINI_API_KEY_EMBED_2` on [1416, 2832)), each with `--context-file data/processed/chunk_contexts.json`. Checkpoints: `data/processed/embed_checkpoint_ctx_a.json` / `_ctx_b.json`.
  - **As of stopping for the night: 1,851/2,832 chunks embedded (65.4%)** - worker A at 943/1416 (66.6%), worker B at 908/1416 (64.1%). Contextualized text is longer per chunk than raw text, so batches hold fewer chunks each and this is running slower than v1's embedding pass took.
  - **Hit the same chronic low-memory kill pattern documented on 2026-09-08, repeatedly** - the harness killed both workers ~10 times over the course of this run for system-wide low memory (confirmed unrelated to these jobs specifically: `Get-Process` showed VS Code windows, Chrome, and WSL as the biggest consumers, ~2-3GB free was typical, dipping to ~1.5-1.9GB right before each kill and recovering to 2.5-5GB within 10-30 min). Each time: checked memory, waited if critically low (<2GB), relaunched both workers with the same `--start`/`--end`/`--checkpoint`/`--api-key-env`/`--context-file` args once recovered. **Zero data loss any time** - checkpointed after every batch, exactly like v1.
  - **To resume tomorrow:** just re-run the same two commands (they'll pick up from `embed_checkpoint_ctx_a.json`/`_ctx_b.json` automatically):
    ```
    QDRANT_COLLECTION=fastapi_corpus_contextual python -m src.index.embed_and_index --start 0 --end 1416 --checkpoint data/processed/embed_checkpoint_ctx_a.json --api-key-env GEMINI_API_KEY_EMBED --context-file data/processed/chunk_contexts.json
    QDRANT_COLLECTION=fastapi_corpus_contextual python -m src.index.embed_and_index --start 1416 --end 2832 --checkpoint data/processed/embed_checkpoint_ctx_b.json --api-key-env GEMINI_API_KEY_EMBED_2 --context-file data/processed/chunk_contexts.json
    ```
    Check free memory first (rule of thumb from 2026-09-08 still holds, maybe tighter given how often this recurred: ideally 3GB+); if a launch gets killed again, just wait a few minutes and relaunch - no manual recovery needed beyond that.

## Current state (as of 2026-09-10)

- **Resumed after the overnight stop.** Docker Desktop had to be restarted (containers had exited); `docker compose up -d` brought `project-qdrant-1` and `project-phoenix-1` back up healthy. Starting Docker Desktop itself caused a sharp transient memory spike (free memory briefly dropped to ~0.5GB, then fluctuated 1.3-1.9GB for several minutes before recovering to ~9GB) - this is the WSL2 utility VM backing Docker Desktop ballooning on startup, not related to this project's own workers. Waited for recovery before launching anything, per the existing rule of thumb.
- **Finished building `fastapi_corpus_contextual`: 2,832/2,832 chunks, confirmed via Qdrant's `points_count`.** Resumed worker A and worker B from their checkpoints (`embed_checkpoint_ctx_a.json`/`_b.json`, at 951/1416 and 910/1416 respectively) using the exact commands logged the previous night - zero data loss, checkpoints picked up cleanly.
  - **New failure mode hit and fixed:** both workers crashed once each on an uncaught `google.genai.errors.ServerError` (503 UNAVAILABLE) - a *different* exception type than the `httpx.TransportError` family already handled, so it wasn't retried, it just crashed the process. Rather than patch the script mid-run, supervised both workers with a small bash loop that watches each PID and relaunches from checkpoint (`--checkpoint` unchanged) whenever the process exits while its checkpoint file still exists (i.e., didn't finish cleanly) - matches the existing "checkpoint after every batch, safe to just relaunch" design. Worth hardening `embed_and_index.py` itself later to catch `google.genai.errors.ServerError` (503) alongside the transport-error retry, so this doesn't need external supervision next time.
- **Started the actual point of this whole exercise: re-running DeepEval against `fastapi_corpus_contextual`** (`EMBED_BACKEND=gemini`, `QDRANT_COLLECTION=fastapi_corpus_contextual`), scoring into a new `data/processed/eval_report_v2_contextual.json` (kept separate from v1's `eval_report.json` so the baseline isn't touched).
  - **Bug found and fixed in `run_deepeval.py`:** `load_all_scored_questions()` always unioned in the main `eval_report.json` regardless of `--report-path`, to stop *parallel shards of the same run* from double-scoring. That logic wrongly applied here too - since v1 already scored all 437 questions (by question text) in the main report, the v2 run immediately said "all already scored, nothing to do" and exited without scoring anything. Added a `--independent-run` flag (`main(check_shared_report=...)`) that skips the main-report check entirely, for exactly this "score the same golden set against a different index for an ablation comparison" case. Sharding behavior (the original use case) is unchanged by default.
  - **Automated the account-chaining that was done manually for v1.** Added a small persistent supervisor loop (outside the repo, just a monitoring/orchestration loop) that runs `run_deepeval.py` in batches of 10, alternating `GEMINI_API_KEY_2` and `GEMINI_API_KEY` for generation/judge, detects the "exhausted or unavailable for today" message and switches accounts automatically, backs off 30 minutes if both are exhausted in a row (quota resets are unpredictable, sometimes within hours per the 2026-09-07/08 notes), and pauses on low system memory. This is meant to carry the v2 eval to 437/437 unattended, the same way v1 got there over ~2 days.
  - **As of stopping to check in: 41/437 scored, running cleanly, no exhaustion hit yet today.**

## Current state (as of 2026-09-11)

- **v2 eval run finished: 437/437 golden examples scored (100%).** Picked up from 41/437 where 2026-09-10 left off.
  - **Fixed a real bug in the account-chaining supervisor before it did damage:** the (outside-the-repo) supervisor script was invoking system `python` instead of the project's `.venv`, so every batch failed instantly with `ModuleNotFoundError` before making any API calls - no data was written or quota spent, verified by checking the report stayed at a constant count and stayed valid JSON across several kill/relaunch cycles. Fixed by pointing the script at `.venv/Scripts/python.exe` explicitly.
  - **Added the two previously-idle embed accounts (`GEMINI_API_KEY_EMBED`, `GEMINI_API_KEY_EMBED_2`) into the same generation/judge rotation**, since they were sitting unused once indexing finished and the free-tier chat-model cap is account-wide, not role-specific. Went from a 2-account rotation to 4, which meant the loop only had to fall back to a 30-min backoff a couple of times instead of repeatedly - all 4 accounts got used in sequence over the run (`GEMINI_API_KEY_2` -> `GEMINI_API_KEY` -> `GEMINI_API_KEY_EMBED` -> `GEMINI_API_KEY_EMBED_2`), each carrying roughly 80-320 examples before exhausting for the day.
  - **Windows process-management gotcha hit twice while restarting the supervisor:** killing only the top-level `bash.exe` PID does not kill its child processes on Windows (no cascading kill like a Unix process group) - orphaned descendants kept the old loop running with stale settings after a "restart." Fix: always enumerate *every* process matching the script/module name and kill them all in one pass, then verify the list is empty, before relaunching. Also tried relaunching via PowerShell's `Start-Process` instead of the working `nohup ... & disown` pattern - that broke the script's environment (both `date` and the internal `powershell`-based memory check came back empty, causing a fast failure loop instead of real 5-minute backoff pauses) - reverted to `nohup`/`disown`, which has consistently worked.
  - No data loss or corruption at any point despite the process churn - report file checked for valid JSON + no duplicate questions after every kill/relaunch cycle.
- **Final v2 result (428/437 with metrics, 9 recorded as `"error"` entries):** Faithfulness avg **0.958** (93.7% pass @0.7), Answer Relevancy avg **0.809** (71.7% pass), Contextual Precision avg **0.262** (20.8% pass), Contextual Recall avg **0.147** (8.6% pass). All-4-metrics-pass rate: **3.5%** (15/428).
- **v1 -> v2 ablation (Contextual Retrieval): every metric improved**, with the two retrieval-specific metrics moving the most in relative terms (Contextual Precision +13.4%, Contextual Recall +14.8%) - consistent with Anthropic's finding that Contextual Retrieval targets retrieval quality specifically, though at much smaller magnitude on this corpus/dataset than their reported ~49% failed-retrieval reduction. Full comparison table in RESOURCES.md. Retrieval quality is still the dominant bottleneck (both metrics under 30% pass rate) even after the improvement.

## Current state (as of 2026-09-11, later same day)

- **Added an optional cross-encoder reranking stage to `src/rag/retriever.py`** (`USE_RERANKER=true`), the retrieval-quality follow-up flagged in next steps below. Over-fetches `RERANK_FETCH_K=20` candidates by embedding similarity, rescores each `(query, chunk)` pair jointly with a small local cross-encoder (`cross-encoder/ms-marco-MiniLM-L-6-v2`, via `sentence-transformers` - already a dependency, no new package needed), returns the top_k by that score. Local/free, so it doesn't compete with the Gemini free-tier quota the eval loop is bottlenecked on. Off by default - existing v1/v2 baselines are unaffected; opt in via the env var, same pattern as `EMBED_BACKEND`.
  - Smoke-tested against `EMBED_BACKEND=local` - correct, grounded, sourced answer; reranked source ordering looked sensible on manual inspection.
  - **Not yet run against the full golden set.** Next step is a v3 DeepEval ablation (`USE_RERANKER=true` on top of the v2 contextual index) to see whether it actually moves Contextual Precision/Recall, following the same pattern as the v1->v2 comparison (separate report path, `--independent-run`).

## Next steps

1. ~~Let the v2 eval run finish~~ - **done, see above.**
2. ~~Compare v1 vs v2 and write up the ablation~~ - **done, see RESOURCES.md.**
3. ~~Add a reranking stage~~ - **implemented (2026-09-11), see above.** Still need to: run the v3 eval ablation (`USE_RERANKER=true`) against the golden set and write up v2->v3 in RESOURCES.md, same pattern as v1->v2.
4. ~~Harden `embed_and_index.py` to also retry on `google.genai.errors.ServerError` (503)~~ - **done (2026-09-11)**, matches the existing `httpx.TransportError` retry pattern.
5. `fastapi_corpus` (v1, Gemini embeddings, no context) is fully indexed alongside `fastapi_corpus_local` - re-run the eval with `EMBED_BACKEND=gemini` on that (non-contextual) collection too and compare local-vs-Gemini embedding quality as a bonus ablation, lower priority
6. Wire up Phoenix tracing for a debugging demo
7. Stretch: AutoRAG-style sweep over chunk size/top_k picked by eval score; CI workflow running `run_deepeval.py` as a regression gate; deploy the FastAPI app
